package com.example.taskappbar.model.Tarefa

import java.util.Date

class Tarefa(var nome:String, var detalhes: String?, var createdDate: Date, var pzoFinal: Date, status: Double){
    }

        var status = 0.0
            get() {return field}
            set(value) {
                field = value
            }

